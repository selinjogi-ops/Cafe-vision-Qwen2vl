torch>=2.2.0
torchvision>=0.17.0
transformers>=4.39.0
accelerate>=0.27.0
safetensors>=0.4.2

ultralytics>=8.1.0
deep-sort-realtime>=1.3.2

opencv-python>=4.8.0
Pillow>=9.5.0
numpy>=1.24.0

*If You Are CPU Only (Recommended Install Step)

Before installing requirements, run:

pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

Then:

pip install -r requirements.txt

*If You Have CUDA (GPU)

Just run:

pip install -r requirements.txt
